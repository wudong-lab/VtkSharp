// <auto-generated/>
// Documentation derived from VTK headers. See VTK-LICENSE.txt.
using System;
using System.Diagnostics;
using System.Runtime.InteropServices;

namespace VtkSharp;

/// <summary>
/// Fill all 2D contours to create polygons
/// </summary>
/// <remarks>
/// <para>
/// vtkContourTriangulator will generate triangles to fill all of the 2D
/// contours in its input. The input to the filter is a set of lines (not
/// polylines) which when joined form loops. The contours may be concave, and
/// may even contain holes i.e. a contour may contain an internal contour that
/// is wound in the opposite direction (as compared to the outer polygon
/// normal) to indicate that it is a hole.
/// </para>
/// <para>
/// Warning:
/// The triangulation of is done in O(n) time for simple convex
/// inputs, but for non-convex inputs the worst-case time is O(n^2*m^2)
/// where n is the number of points and m is the number of holes.
/// The best triangulation algorithms, in contrast, are O(n log n).
/// The resulting triangles may be quite narrow, the algorithm does
/// not attempt to produce high-quality triangles.
/// </para>
/// <para>
/// See also: vtkClipClosedSurface vtkPolygon
/// </para>
/// </remarks>
public unsafe partial class vtkContourTriangulator : vtkPolyDataAlgorithm
{
    protected vtkContourTriangulator(nint nativePointer, bool ownsReference) : base(nativePointer, ownsReference) { }
    /// <summary>
    /// Standard methods for instantiation, obtaining type information, and printing.
    /// </summary>
    /// <remarks>
    /// The C# wrapper owns a native reference. Call Dispose() when finished to release that reference.
    /// </remarks>
    public new static vtkContourTriangulator New() => new(vtkContourTriangulator_New(), ownsReference: true);
    /// <summary>
    /// Wraps a live native object without adding a reference or taking ownership.
    /// </summary>
    /// <remarks>
    /// Dispose() does not release the borrowed reference. Keep the native object alive while using this wrapper.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type.
    /// </param>
    /// <returns>
    /// A wrapper that does not own a native reference.
    /// </returns>
    public new static vtkContourTriangulator FromBorrowedPointer(nint nativePointer) => new(nativePointer, ownsReference: false);
    /// <summary>
    /// Takes ownership of one existing native reference without incrementing the reference count.
    /// </summary>
    /// <remarks>
    /// The caller transfers responsibility for releasing this reference to the wrapper; do not release it separately or transfer it twice. Call Dispose() when finished.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type, with one owned reference to transfer.
    /// </param>
    /// <returns>
    /// A wrapper that releases the transferred reference on Dispose().
    /// </returns>
    public new static vtkContourTriangulator TakeReference(nint nativePointer) => new(nativePointer, ownsReference: true);

    /// <summary>
    /// Creates another wrapper for the same native object and increments its reference count by one.
    /// </summary>
    /// <remarks>
    /// The source wrapper's ownership is unchanged. Call Dispose() on the returned wrapper to release the additional reference. This does not copy the native object.
    /// </remarks>
    /// <param name="sourceObject">
    /// A wrapper for a native object that is still alive.
    /// </param>
    /// <returns>
    /// A wrapper that owns the additional native reference independently of the source wrapper.
    /// </returns>
    public new static vtkContourTriangulator Register(vtkContourTriangulator sourceObject)
    {
        var target = new vtkContourTriangulator(sourceObject.NativePointer, true);
        target.Register();
        return target;
    }

    #region Interop
    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern nint vtkContourTriangulator_New();
    #endregion
}
