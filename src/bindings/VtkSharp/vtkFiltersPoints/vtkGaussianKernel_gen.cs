// <auto-generated/>
// Documentation derived from VTK headers. See VTK-LICENSE.txt.
using System;
using System.Diagnostics;
using System.Runtime.InteropServices;

namespace VtkSharp;

/// <summary>
/// a spherical Gaussian interpolation kernel
/// </summary>
/// <remarks>
/// <para>
///
/// vtkGaussianKernel is an interpolation kernel that simply returns the
/// weights for all points found in the sphere defined by radius R. The
/// weights are computed as: exp(-(s*r/R)^2) where r is the distance from the
/// point to be interpolated to a neighboring point within R. The sharpness s
/// simply affects the rate of fall off of the Gaussian. (A more general
/// Gaussian kernel is available from vtkEllipsoidalGaussianKernel.)
/// </para>
/// <para>
/// Warning:
/// The weights are normalized sp that SUM(Wi) = 1. If a neighbor point p
/// precisely lies on the point to be interpolated, then the interpolated
/// point takes on the values associated with p.
/// </para>
/// <para>
/// See also: vtkPointInterpolator vtkInterpolationKernel vtkEllipsoidalGaussianKernel
/// vtkVoronoiKernel vtkSPHKernel vtkShepardKernel
/// </para>
/// </remarks>
public unsafe partial class vtkGaussianKernel : vtkGeneralizedKernel
{
    protected vtkGaussianKernel(nint nativePointer, bool ownsReference) : base(nativePointer, ownsReference) { }
    /// <summary>
    /// Standard methods for instantiation, obtaining type information, and printing.
    /// </summary>
    /// <remarks>
    /// The C# wrapper owns a native reference. Call Dispose() when finished to release that reference.
    /// </remarks>
    public new static vtkGaussianKernel New() => new(vtkGaussianKernel_New(), ownsReference: true);
    /// <summary>
    /// Wraps a live native object without adding a reference or taking ownership.
    /// </summary>
    /// <remarks>
    /// Dispose() does not release the borrowed reference. Keep the native object alive while using this wrapper.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type.
    /// </param>
    /// <returns>
    /// A wrapper that does not own a native reference.
    /// </returns>
    public new static vtkGaussianKernel FromBorrowedPointer(nint nativePointer) => new(nativePointer, ownsReference: false);
    /// <summary>
    /// Takes ownership of one existing native reference without incrementing the reference count.
    /// </summary>
    /// <remarks>
    /// The caller transfers responsibility for releasing this reference to the wrapper; do not release it separately or transfer it twice. Call Dispose() when finished.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type, with one owned reference to transfer.
    /// </param>
    /// <returns>
    /// A wrapper that releases the transferred reference on Dispose().
    /// </returns>
    public new static vtkGaussianKernel TakeReference(nint nativePointer) => new(nativePointer, ownsReference: true);

    /// <summary>
    /// Creates another wrapper for the same native object and increments its reference count by one.
    /// </summary>
    /// <remarks>
    /// The source wrapper's ownership is unchanged. Call Dispose() on the returned wrapper to release the additional reference. This does not copy the native object.
    /// </remarks>
    /// <param name="sourceObject">
    /// A wrapper for a native object that is still alive.
    /// </param>
    /// <returns>
    /// A wrapper that owns the additional native reference independently of the source wrapper.
    /// </returns>
    public new static vtkGaussianKernel Register(vtkGaussianKernel sourceObject)
    {
        var target = new vtkGaussianKernel(sourceObject.NativePointer, true);
        target.Register();
        return target;
    }

    /// <summary>
    /// Set / Get the sharpness (i.e., falloff) of the Gaussian. By default
    /// Sharpness=2. As the sharpness increases the effects of distant points
    /// are reduced.
    /// </summary>
    public new void SetSharpness(double _arg)
    {
        vtkGaussianKernel_SetSharpness(this.NativePointer, _arg);
    }

    #region Interop
    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern nint vtkGaussianKernel_New();

    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern void vtkGaussianKernel_SetSharpness(nint self, double _arg);
    #endregion
}
