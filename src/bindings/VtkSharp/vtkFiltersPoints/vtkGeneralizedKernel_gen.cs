// <auto-generated/>
// Documentation derived from VTK headers. See VTK-LICENSE.txt.
using System;
using System.Diagnostics;
using System.Runtime.InteropServices;

namespace VtkSharp;

/// <summary>
/// flexible, general interpolation kernels
/// </summary>
/// <remarks>
/// <para>
///
/// vtkGeneralizedKernel is an abstract class that defines an API for concrete
/// general-purpose, kernel subclasses. vtkGeneralizedKernels has important
/// properties that make them useful in a variety of interpolation
/// applications:
/// &lt;pre&gt;
/// 1. The weights are normalized.
/// 2. The footprint of the basis is configurable.
/// 3. Probabilistic weighting functions can be used to favor certain weights.
/// &lt;/pre&gt;
/// The following paragraphs describe each of these properties in more detail.
/// </para>
/// <para>
/// Normalized weightings simple mean Sum(w_i) = 1. This ensures that the
/// interpolation process is well behaved.
/// </para>
/// <para>
/// The interpolation footprint is the set of points that are used to perform
/// the interpolation process. For example, it is possible to choose between a
/// radius-based kernel selection, and one based on the N nearest
/// neighbors. Note that the performance and mathematical properties of
/// kernels may vary greatly depending on which kernel style is selected. For
/// example, if a radius-based kernel footprint is used, and the radius is too
/// big, the algorithm can perform in n^3 fashion.
/// </para>
/// <para>
/// Finally, in advanced usage, probability functions can be applied to the
/// interpolation weights (prior to normalization). These probability
/// functions are confidence estimates that the data at a particular point is
/// accurate. A typical application is when laser scans are used to acquire
/// point measurements, which return normals that indicate glancing returns
/// versus direct, near orthogonal hits. Another use is when point clouds are
/// combined, where some clouds are acquired with more accurate, detailed
/// devices versus a broad, potentially coarser acquisition process.
/// </para>
/// <para>
/// Warning:
/// Some kernels, like the Voronoi kernel, cannot be subclasses of this class
/// because their definition inherently defines the basis style. For example,
/// the Voronoi kernel is simply the single closest point. SPH kernels are
/// similar, because they implicitly depend on a particle distribution
/// consistent with simulation constraints such as conservation of mass, etc.
/// </para>
/// <para>
/// See also: vtkPointInterpolator vtkPointInterpolator2D vtkGaussianKernel vtkSPHKernel
/// vtkShepardKernel vtkLinearKernel vtkVoronoiKernel
/// </para>
/// </remarks>
public unsafe partial class vtkGeneralizedKernel : vtkInterpolationKernel
{
    protected vtkGeneralizedKernel(nint nativePointer, bool ownsReference) : base(nativePointer, ownsReference) { }
    /// <summary>
    /// Wraps a live native object without adding a reference or taking ownership.
    /// </summary>
    /// <remarks>
    /// Dispose() does not release the borrowed reference. Keep the native object alive while using this wrapper.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type.
    /// </param>
    /// <returns>
    /// A wrapper that does not own a native reference.
    /// </returns>
    public new static vtkGeneralizedKernel FromBorrowedPointer(nint nativePointer) => new(nativePointer, ownsReference: false);
    /// <summary>
    /// Takes ownership of one existing native reference without incrementing the reference count.
    /// </summary>
    /// <remarks>
    /// The caller transfers responsibility for releasing this reference to the wrapper; do not release it separately or transfer it twice. Call Dispose() when finished.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type, with one owned reference to transfer.
    /// </param>
    /// <returns>
    /// A wrapper that releases the transferred reference on Dispose().
    /// </returns>
    public new static vtkGeneralizedKernel TakeReference(nint nativePointer) => new(nativePointer, ownsReference: true);

    /// <summary>
    /// Creates another wrapper for the same native object and increments its reference count by one.
    /// </summary>
    /// <remarks>
    /// The source wrapper's ownership is unchanged. Call Dispose() on the returned wrapper to release the additional reference. This does not copy the native object.
    /// </remarks>
    /// <param name="sourceObject">
    /// A wrapper for a native object that is still alive.
    /// </param>
    /// <returns>
    /// A wrapper that owns the additional native reference independently of the source wrapper.
    /// </returns>
    public new static vtkGeneralizedKernel Register(vtkGeneralizedKernel sourceObject)
    {
        var target = new vtkGeneralizedKernel(sourceObject.NativePointer, true);
        target.Register();
        return target;
    }

    /// <summary>
    /// If the interpolation basis style is Radius, then this method specifies
    /// the radius within which the basis points must lie.
    /// </summary>
    public new void SetRadius(double _arg)
    {
        vtkGeneralizedKernel_SetRadius(this.NativePointer, _arg);
    }

    #region Interop
    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern void vtkGeneralizedKernel_SetRadius(nint self, double _arg);
    #endregion
}
