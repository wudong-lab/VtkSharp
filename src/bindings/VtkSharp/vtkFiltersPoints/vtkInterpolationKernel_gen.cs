// <auto-generated/>
// Documentation derived from VTK headers. See VTK-LICENSE.txt.
using System;
using System.Diagnostics;
using System.Runtime.InteropServices;

namespace VtkSharp;

/// <summary>
/// base class for interpolation kernels
/// </summary>
/// <remarks>
/// <para>
///
/// vtkInterpolationKernel specifies an abstract interface for interpolation
/// kernels. An interpolation kernel is used to produce an interpolated data
/// value at a point X from the points + data in a local neighborhood
/// surrounding X. For example, given the N nearest points surrounding X, the
/// interpolation kernel provides N weights, which when combined with the N
/// data values associated with these nearest points, produces an interpolated
/// data value at point X.
/// </para>
/// <para>
/// Note that various kernel initialization methods are provided. The basic
/// method requires providing a point locator to accelerate neighborhood
/// queries. Some kernels may refer back to the original dataset, or the point
/// attribute data associated with the dataset. The initialization method
/// enables different styles of initialization and is kernel-dependent.
/// </para>
/// <para>
/// Typically the kernels are invoked in two parts: first, the basis is
/// computed using the supplied point locator and dataset. This basis is a
/// local footprint of point surrounding a poitnX. In this footprint are the
/// neighboring points used to compute the interpolation weights. Then, the
/// weights are computed from points forming the basis. However, advanced
/// users can develop their own basis, skipping the ComputeBasis() method, and
/// then invoke ComputeWeights() directly.
/// </para>
/// <para>
/// Warning:
/// The ComputeBasis() and ComputeWeights() methods must be thread safe as they
/// are used in threaded algorithms.
/// </para>
/// <para>
/// See also: vtkPointInterpolator vtkPointInterpolator2D vtkGeneralizedKernel
/// vtkGaussianKernel vtkSPHKernel vtkShepardKernel vtkVoronoiKernel
/// </para>
/// </remarks>
public unsafe partial class vtkInterpolationKernel : vtkObject
{
    protected vtkInterpolationKernel(nint nativePointer, bool ownsReference) : base(nativePointer, ownsReference) { }
    /// <summary>
    /// Wraps a live native object without adding a reference or taking ownership.
    /// </summary>
    /// <remarks>
    /// Dispose() does not release the borrowed reference. Keep the native object alive while using this wrapper.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type.
    /// </param>
    /// <returns>
    /// A wrapper that does not own a native reference.
    /// </returns>
    public new static vtkInterpolationKernel FromBorrowedPointer(nint nativePointer) => new(nativePointer, ownsReference: false);
    /// <summary>
    /// Takes ownership of one existing native reference without incrementing the reference count.
    /// </summary>
    /// <remarks>
    /// The caller transfers responsibility for releasing this reference to the wrapper; do not release it separately or transfer it twice. Call Dispose() when finished.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type, with one owned reference to transfer.
    /// </param>
    /// <returns>
    /// A wrapper that releases the transferred reference on Dispose().
    /// </returns>
    public new static vtkInterpolationKernel TakeReference(nint nativePointer) => new(nativePointer, ownsReference: true);

    /// <summary>
    /// Creates another wrapper for the same native object and increments its reference count by one.
    /// </summary>
    /// <remarks>
    /// The source wrapper's ownership is unchanged. Call Dispose() on the returned wrapper to release the additional reference. This does not copy the native object.
    /// </remarks>
    /// <param name="sourceObject">
    /// A wrapper for a native object that is still alive.
    /// </param>
    /// <returns>
    /// A wrapper that owns the additional native reference independently of the source wrapper.
    /// </returns>
    public new static vtkInterpolationKernel Register(vtkInterpolationKernel sourceObject)
    {
        var target = new vtkInterpolationKernel(sourceObject.NativePointer, true);
        target.Register();
        return target;
    }

    #region Interop
    #endregion
}
