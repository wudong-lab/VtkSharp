// <auto-generated/>
// Documentation derived from VTK headers. See VTK-LICENSE.txt.
using System;
using System.Diagnostics;
using System.Runtime.InteropServices;

namespace VtkSharp;

/// <summary>
/// reads in delimited ascii or unicode text files
/// and outputs a vtkTable data structure.
/// </summary>
/// <remarks>
/// <para>
///
/// vtkDelimitedTextReader is an interface for pulling in data from a
/// flat, delimited ascii or unicode text file (delimiter can be any character).
/// </para>
/// <para>
/// The behavior of the reader with respect to ascii or unicode input
/// is controlled by the SetUnicodeCharacterSet() method.  By default
/// (without calling SetUnicodeCharacterSet()), the reader will expect
/// to read ascii text and will output vtkStdString columns.  Use the
/// Set and Get methods to set delimiters that do not contain UTF8 in
/// the name when operating the reader in default ascii mode.  If the
/// SetUnicodeCharacterSet() method is called, the reader will output
/// vtkStdString columns in the output table.  In addition, it is
/// necessary to use the Set and Get methods that contain UTF8 in the
/// name to specify delimiters when operating in unicode mode.
/// </para>
/// <para>
/// There is also a special character set US-ASCII-WITH-FALLBACK that
/// will treat the input text as ASCII no matter what.  If and when it
/// encounters a character with its 8th bit set it will replace that
/// character with the code point ReplacementCharacter.  You may use
/// this if you have text that belongs to a code page like LATIN9 or
/// ISO-8859-1 or friends: mostly ASCII but not entirely.  Eventually
/// this class will acquire the ability to read gracefully text from
/// any code page, making this option obsolete.
/// </para>
/// <para>
/// This class emits ProgressEvent for every 100 lines it reads.
/// </para>
/// <para>
/// Warning:
/// This reader assumes that the first line in the file (whether that's
/// headers or the first document) contains at least as many fields as
/// any other line in the file.
/// </para>
/// </remarks>
public unsafe partial class vtkDelimitedTextReader : vtkTableAlgorithm
{
    protected vtkDelimitedTextReader(nint nativePointer, bool ownsReference) : base(nativePointer, ownsReference) { }
    /// <remarks>
    /// The C# wrapper owns a native reference. Call Dispose() when finished to release that reference.
    /// </remarks>
    public new static vtkDelimitedTextReader New() => new(vtkDelimitedTextReader_New(), ownsReference: true);
    /// <summary>
    /// Wraps a live native object without adding a reference or taking ownership.
    /// </summary>
    /// <remarks>
    /// Dispose() does not release the borrowed reference. Keep the native object alive while using this wrapper.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type.
    /// </param>
    /// <returns>
    /// A wrapper that does not own a native reference.
    /// </returns>
    public new static vtkDelimitedTextReader FromBorrowedPointer(nint nativePointer) => new(nativePointer, ownsReference: false);
    /// <summary>
    /// Takes ownership of one existing native reference without incrementing the reference count.
    /// </summary>
    /// <remarks>
    /// The caller transfers responsibility for releasing this reference to the wrapper; do not release it separately or transfer it twice. Call Dispose() when finished.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type, with one owned reference to transfer.
    /// </param>
    /// <returns>
    /// A wrapper that releases the transferred reference on Dispose().
    /// </returns>
    public new static vtkDelimitedTextReader TakeReference(nint nativePointer) => new(nativePointer, ownsReference: true);

    /// <summary>
    /// Creates another wrapper for the same native object and increments its reference count by one.
    /// </summary>
    /// <remarks>
    /// The source wrapper's ownership is unchanged. Call Dispose() on the returned wrapper to release the additional reference. This does not copy the native object.
    /// </remarks>
    /// <param name="sourceObject">
    /// A wrapper for a native object that is still alive.
    /// </param>
    /// <returns>
    /// A wrapper that owns the additional native reference independently of the source wrapper.
    /// </returns>
    public new static vtkDelimitedTextReader Register(vtkDelimitedTextReader sourceObject)
    {
        var target = new vtkDelimitedTextReader(sourceObject.NativePointer, true);
        target.Register();
        return target;
    }

    /// <summary>
    /// When set to true, the reader will detect numeric columns and create
    /// vtkDoubleArray or vtkIntArray for those instead of vtkStringArray. Default
    /// is off.
    /// Then, it works as follow:
    /// - uses vtkIntArray
    /// - if data is not an int, try vtkDoubleArray
    /// - if data is not a double, fallback to vtkStringArray
    /// </summary>
    public new void DetectNumericColumnsOn()
    {
        vtkDelimitedTextReader_DetectNumericColumnsOn(this.NativePointer);
    }

    /// <summary>
    /// Specify the character(s) that will be used to separate fields.  For
    /// example, set this to "," for a comma-separated value file.  Set
    /// it to ".:;" for a file where columns can be separated by a
    /// period, colon or semicolon.  The order of the characters in the
    /// string does not matter.  Defaults to a comma.
    /// </summary>
    public new void SetFieldDelimiterCharacters(string _arg)
    {
        #if NET8_0_OR_GREATER
        vtkDelimitedTextReader_SetFieldDelimiterCharacters(this.NativePointer, _arg);
        #else
        vtkDelimitedTextReader_SetFieldDelimiterCharacters(this.NativePointer, VtkString.ToNullTerminatedUtf8(_arg));
        #endif
    }

    /// <summary>
    /// Specifies the delimited text file to be loaded.
    /// </summary>
    public new void SetFileName(string _arg)
    {
        #if NET8_0_OR_GREATER
        vtkDelimitedTextReader_SetFileName(this.NativePointer, _arg);
        #else
        vtkDelimitedTextReader_SetFileName(this.NativePointer, VtkString.ToNullTerminatedUtf8(_arg));
        #endif
    }

    /// <summary>
    /// Set/get whether to treat the first line of the file as headers.
    /// The default is false (no headers).
    /// </summary>
    public new void SetHaveHeaders(bool _arg)
    {
        vtkDelimitedTextReader_SetHaveHeaders(this.NativePointer, _arg);
    }

    #region Interop
    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern nint vtkDelimitedTextReader_New();

    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern void vtkDelimitedTextReader_DetectNumericColumnsOn(nint self);

#if NET8_0_OR_GREATER
    [LibraryImport(InteropInfo.NativeLibraryName, StringMarshalling = StringMarshalling.Utf8)]
    private static partial void vtkDelimitedTextReader_SetFieldDelimiterCharacters(nint self, string _arg);
#else
    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern void vtkDelimitedTextReader_SetFieldDelimiterCharacters(nint self, byte[] _arg);
#endif

#if NET8_0_OR_GREATER
    [LibraryImport(InteropInfo.NativeLibraryName, StringMarshalling = StringMarshalling.Utf8)]
    private static partial void vtkDelimitedTextReader_SetFileName(nint self, string _arg);
#else
    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern void vtkDelimitedTextReader_SetFileName(nint self, byte[] _arg);
#endif

    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern void vtkDelimitedTextReader_SetHaveHeaders(nint self, [MarshalAs(UnmanagedType.U1)] bool _arg);
    #endregion
}
