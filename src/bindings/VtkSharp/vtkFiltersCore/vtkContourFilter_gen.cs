// <auto-generated/>
// Documentation derived from VTK headers. See VTK-LICENSE.txt.
using System;
using System.Diagnostics;
using System.Runtime.InteropServices;

namespace VtkSharp;

/// <summary>
/// generate isosurfaces/isolines from scalar values
/// </summary>
/// <remarks>
/// <para>
/// vtkContourFilter is a filter that takes as input any dataset and
/// generates on output isosurfaces and/or isolines. The exact form
/// of the output depends upon the dimensionality of the input data.
/// Data consisting of 3D cells will generate isosurfaces, data
/// consisting of 2D cells will generate isolines, and data with 1D
/// or 0D cells will generate isopoints. Combinations of output type
/// are possible if the input dimension is mixed.
/// </para>
/// <para>
/// To use this filter you must specify one or more contour values.
/// You can either use the method SetValue() to specify each contour
/// value, or use GenerateValues() to generate a series of evenly
/// spaced contours. It is also possible to accelerate the operation of
/// this filter (at the cost of extra memory) by using a
/// vtkScalarTree. A scalar tree is used to quickly locate cells that
/// contain a contour surface. This is especially effective if multiple
/// contours are being extracted. If you want to use a scalar tree,
/// invoke the method UseScalarTreeOn().
/// </para>
/// <para>
/// Warning:
/// For unstructured data or structured grids, normals and gradients
/// are not computed. Use vtkPolyDataNormals to compute the surface
/// normals.
/// </para>
/// <para>
/// See also: vtkFlyingEdges3D vtkFlyingEdges2D vtkDiscreteFlyingEdges3D
/// vtkDiscreteFlyingEdges2D vtkMarchingContourFilter vtkMarchingCubes
/// vtkSliceCubes vtkMarchingSquares vtkImageMarchingCubes vtkContour3DLinearGrid
/// </para>
/// </remarks>
public unsafe partial class vtkContourFilter : vtkPolyDataAlgorithm
{
    protected vtkContourFilter(nint nativePointer, bool ownsReference) : base(nativePointer, ownsReference) { }
    /// <summary>
    /// Construct object with initial range (0,1) and single contour value
    /// of 0.0.
    /// </summary>
    /// <remarks>
    /// The C# wrapper owns a native reference. Call Dispose() when finished to release that reference.
    /// </remarks>
    public new static vtkContourFilter New() => new(vtkContourFilter_New(), ownsReference: true);
    /// <summary>
    /// Wraps a live native object without adding a reference or taking ownership.
    /// </summary>
    /// <remarks>
    /// Dispose() does not release the borrowed reference. Keep the native object alive while using this wrapper.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type.
    /// </param>
    /// <returns>
    /// A wrapper that does not own a native reference.
    /// </returns>
    public new static vtkContourFilter FromBorrowedPointer(nint nativePointer) => new(nativePointer, ownsReference: false);
    /// <summary>
    /// Takes ownership of one existing native reference without incrementing the reference count.
    /// </summary>
    /// <remarks>
    /// The caller transfers responsibility for releasing this reference to the wrapper; do not release it separately or transfer it twice. Call Dispose() when finished.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type, with one owned reference to transfer.
    /// </param>
    /// <returns>
    /// A wrapper that releases the transferred reference on Dispose().
    /// </returns>
    public new static vtkContourFilter TakeReference(nint nativePointer) => new(nativePointer, ownsReference: true);

    /// <summary>
    /// Creates another wrapper for the same native object and increments its reference count by one.
    /// </summary>
    /// <remarks>
    /// The source wrapper's ownership is unchanged. Call Dispose() on the returned wrapper to release the additional reference. This does not copy the native object.
    /// </remarks>
    /// <param name="sourceObject">
    /// A wrapper for a native object that is still alive.
    /// </param>
    /// <returns>
    /// A wrapper that owns the additional native reference independently of the source wrapper.
    /// </returns>
    public new static vtkContourFilter Register(vtkContourFilter sourceObject)
    {
        var target = new vtkContourFilter(sourceObject.NativePointer, true);
        target.Register();
        return target;
    }

    /// <summary>
    /// Methods to set / get contour values.
    /// </summary>
    public new void GenerateValues(int numContours, double rangeStart, double rangeEnd)
    {
        vtkContourFilter_GenerateValues(this.NativePointer, numContours, rangeStart, rangeEnd);
    }

    #region Interop
    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern nint vtkContourFilter_New();

    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern void vtkContourFilter_GenerateValues(nint self, int numContours, double rangeStart, double rangeEnd);
    #endregion
}
