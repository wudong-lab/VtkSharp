// <auto-generated/>
// Documentation derived from VTK headers. See VTK-LICENSE.txt.
using System;
using System.Diagnostics;
using System.Runtime.InteropServices;

namespace VtkSharp;

/// <summary>
/// create triangle strips and/or poly-lines
/// </summary>
/// <remarks>
/// <para>
///
/// vtkStripper is a filter that generates triangle strips and/or poly-lines
/// from input polygons, triangle strips, and lines. Input polygons are
/// assembled into triangle strips only if they are triangles; other types of
/// polygons are passed through to the output and not stripped. (Use
/// vtkTriangleFilter to triangulate non-triangular polygons prior to running
/// this filter if you need to strip all the data.) The filter will pass
/// through (to the output) vertices if they are present in the input
/// polydata. Also note that if triangle strips or polylines are defined in
/// the input they are passed through and not joined nor extended. (If you wish
/// to strip these use vtkTriangleFilter to fragment the input into triangles
/// and lines prior to running vtkStripper.)
/// </para>
/// <para>
/// The ivar MaximumLength can be used to control the maximum
/// allowable triangle strip and poly-line length.
/// </para>
/// <para>
/// By default, this filter discards any cell data associated with the input.
/// Thus is because the cell structure changes and and the old cell data
/// is no longer valid. When PassCellDataAsFieldData flag is set,
/// the cell data is passed as FieldData to the output using the following rule:
/// 1) for every cell in the output that is not a triangle strip,
/// the cell data is inserted once per cell in the output field data.
/// 2) for every triangle strip cell in the output:
/// ii) 1 tuple is inserted for every point(j|j&gt;=2) in the strip.
/// This is the cell data for the cell formed by (j-2, j-1, j) in
/// the input.
/// The field data order is same as cell data i.e. (verts,line,polys,tsrips).
/// </para>
/// <para>
/// If there is a ghost cell array in the input, the ghost array is discarded.
/// Any cell tagged as ghost is skipped when stripping. Ghost points are kept.
/// </para>
/// <para>
/// Warning:
/// If triangle strips or poly-lines exist in the input data they will
/// be passed through to the output data. This filter will only construct
/// triangle strips if triangle polygons are available; and will only
/// construct poly-lines if lines are available.
/// </para>
/// <para>
/// See also: vtkTriangleFilter
/// </para>
/// </remarks>
public unsafe partial class vtkStripper : vtkPolyDataAlgorithm
{
    protected vtkStripper(nint nativePointer, bool ownsReference) : base(nativePointer, ownsReference) { }
    /// <summary>
    /// Construct object with MaximumLength set to 1000.
    /// </summary>
    /// <remarks>
    /// The C# wrapper owns a native reference. Call Dispose() when finished to release that reference.
    /// </remarks>
    public new static vtkStripper New() => new(vtkStripper_New(), ownsReference: true);
    /// <summary>
    /// Wraps a live native object without adding a reference or taking ownership.
    /// </summary>
    /// <remarks>
    /// Dispose() does not release the borrowed reference. Keep the native object alive while using this wrapper.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type.
    /// </param>
    /// <returns>
    /// A wrapper that does not own a native reference.
    /// </returns>
    public new static vtkStripper FromBorrowedPointer(nint nativePointer) => new(nativePointer, ownsReference: false);
    /// <summary>
    /// Takes ownership of one existing native reference without incrementing the reference count.
    /// </summary>
    /// <remarks>
    /// The caller transfers responsibility for releasing this reference to the wrapper; do not release it separately or transfer it twice. Call Dispose() when finished.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type, with one owned reference to transfer.
    /// </param>
    /// <returns>
    /// A wrapper that releases the transferred reference on Dispose().
    /// </returns>
    public new static vtkStripper TakeReference(nint nativePointer) => new(nativePointer, ownsReference: true);

    /// <summary>
    /// Creates another wrapper for the same native object and increments its reference count by one.
    /// </summary>
    /// <remarks>
    /// The source wrapper's ownership is unchanged. Call Dispose() on the returned wrapper to release the additional reference. This does not copy the native object.
    /// </remarks>
    /// <param name="sourceObject">
    /// A wrapper for a native object that is still alive.
    /// </param>
    /// <returns>
    /// A wrapper that owns the additional native reference independently of the source wrapper.
    /// </returns>
    public new static vtkStripper Register(vtkStripper sourceObject)
    {
        var target = new vtkStripper(sourceObject.NativePointer, true);
        target.Register();
        return target;
    }

    #region Interop
    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern nint vtkStripper_New();
    #endregion
}
