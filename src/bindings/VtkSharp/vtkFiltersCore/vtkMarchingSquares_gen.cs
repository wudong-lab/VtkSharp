// <auto-generated/>
// Documentation derived from VTK headers. See VTK-LICENSE.txt.
using System;
using System.Diagnostics;
using System.Runtime.InteropServices;

namespace VtkSharp;

/// <summary>
/// generate isoline(s) from structured points set
/// </summary>
/// <remarks>
/// <para>
/// vtkMarchingSquares is a filter that takes as input a structured points set
/// and generates on output one or more isolines.  One or more contour values
/// must be specified to generate the isolines.  Alternatively, you can specify
/// a min/max scalar range and the number of contours to generate a series of
/// evenly spaced contour values.
/// </para>
/// <para>
/// To generate contour lines the input data must be of topological dimension 2
/// (i.e., an image). If not, you can use the ImageRange ivar to select an
/// image plane from an input volume. This avoids having to extract a plane first
/// (using vtkExtractSubVolume).  The filter deals with this by first
/// trying to use the input data directly, and if not a 2D image, then uses the
/// ImageRange ivar to reduce it to an image.
/// </para>
/// <para>
/// Warning:
/// This filter is specialized to images. If you are interested in
/// contouring other types of data, use the general vtkContourFilter.
/// </para>
/// <para>
/// See also: vtkContourFilter vtkMarchingCubes vtkSliceCubes vtkDividingCubes
/// </para>
/// </remarks>
public unsafe partial class vtkMarchingSquares : vtkPolyDataAlgorithm
{
    protected vtkMarchingSquares(nint nativePointer, bool ownsReference) : base(nativePointer, ownsReference) { }
    /// <remarks>
    /// The C# wrapper owns a native reference. Call Dispose() when finished to release that reference.
    /// </remarks>
    public new static vtkMarchingSquares New() => new(vtkMarchingSquares_New(), ownsReference: true);
    /// <summary>
    /// Wraps a live native object without adding a reference or taking ownership.
    /// </summary>
    /// <remarks>
    /// Dispose() does not release the borrowed reference. Keep the native object alive while using this wrapper.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type.
    /// </param>
    /// <returns>
    /// A wrapper that does not own a native reference.
    /// </returns>
    public new static vtkMarchingSquares FromBorrowedPointer(nint nativePointer) => new(nativePointer, ownsReference: false);
    /// <summary>
    /// Takes ownership of one existing native reference without incrementing the reference count.
    /// </summary>
    /// <remarks>
    /// The caller transfers responsibility for releasing this reference to the wrapper; do not release it separately or transfer it twice. Call Dispose() when finished.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type, with one owned reference to transfer.
    /// </param>
    /// <returns>
    /// A wrapper that releases the transferred reference on Dispose().
    /// </returns>
    public new static vtkMarchingSquares TakeReference(nint nativePointer) => new(nativePointer, ownsReference: true);

    /// <summary>
    /// Creates another wrapper for the same native object and increments its reference count by one.
    /// </summary>
    /// <remarks>
    /// The source wrapper's ownership is unchanged. Call Dispose() on the returned wrapper to release the additional reference. This does not copy the native object.
    /// </remarks>
    /// <param name="sourceObject">
    /// A wrapper for a native object that is still alive.
    /// </param>
    /// <returns>
    /// A wrapper that owns the additional native reference independently of the source wrapper.
    /// </returns>
    public new static vtkMarchingSquares Register(vtkMarchingSquares sourceObject)
    {
        var target = new vtkMarchingSquares(sourceObject.NativePointer, true);
        target.Register();
        return target;
    }

    /// <summary>
    /// Methods to set contour values
    /// </summary>
    public new void SetValue(int i, double value)
    {
        vtkMarchingSquares_SetValue(this.NativePointer, i, value);
    }

    #region Interop
    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern nint vtkMarchingSquares_New();

    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern void vtkMarchingSquares_SetValue(nint self, int i, double value);
    #endregion
}
