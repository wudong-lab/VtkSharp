// <auto-generated/>
// Documentation derived from VTK headers. See VTK-LICENSE.txt.
using System;
using System.Diagnostics;
using System.Runtime.InteropServices;

namespace VtkSharp;

/// <summary>
/// read PNG files
/// </summary>
/// <remarks>
/// <para>
/// vtkPNGReader is a source object that reads PNG files.
/// It should be able to read most any PNG file
/// </para>
/// <para>
/// This reader supports the stream API.
/// </para>
/// <para>
/// See also: vtkPNGWriter
/// </para>
/// </remarks>
public unsafe partial class vtkPNGReader : vtkImageReader2
{
    protected vtkPNGReader(nint nativePointer, bool ownsReference) : base(nativePointer, ownsReference) { }
    /// <remarks>
    /// The C# wrapper owns a native reference. Call Dispose() when finished to release that reference.
    /// </remarks>
    public new static vtkPNGReader New() => new(vtkPNGReader_New(), ownsReference: true);
    /// <summary>
    /// Wraps a live native object without adding a reference or taking ownership.
    /// </summary>
    /// <remarks>
    /// Dispose() does not release the borrowed reference. Keep the native object alive while using this wrapper.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type.
    /// </param>
    /// <returns>
    /// A wrapper that does not own a native reference.
    /// </returns>
    public new static vtkPNGReader FromBorrowedPointer(nint nativePointer) => new(nativePointer, ownsReference: false);
    /// <summary>
    /// Takes ownership of one existing native reference without incrementing the reference count.
    /// </summary>
    /// <remarks>
    /// The caller transfers responsibility for releasing this reference to the wrapper; do not release it separately or transfer it twice. Call Dispose() when finished.
    /// </remarks>
    /// <param name="nativePointer">
    /// A non-null pointer to a live native object of the corresponding VTK type, with one owned reference to transfer.
    /// </param>
    /// <returns>
    /// A wrapper that releases the transferred reference on Dispose().
    /// </returns>
    public new static vtkPNGReader TakeReference(nint nativePointer) => new(nativePointer, ownsReference: true);

    /// <summary>
    /// Creates another wrapper for the same native object and increments its reference count by one.
    /// </summary>
    /// <remarks>
    /// The source wrapper's ownership is unchanged. Call Dispose() on the returned wrapper to release the additional reference. This does not copy the native object.
    /// </remarks>
    /// <param name="sourceObject">
    /// A wrapper for a native object that is still alive.
    /// </param>
    /// <returns>
    /// A wrapper that owns the additional native reference independently of the source wrapper.
    /// </returns>
    public new static vtkPNGReader Register(vtkPNGReader sourceObject)
    {
        var target = new vtkPNGReader(sourceObject.NativePointer, true);
        target.Register();
        return target;
    }

    /// <summary>
    /// Return 3 if, after a quick check of file header, it looks like the provided file or stream
    /// can be read as a png file. Return 0 if it is sure it cannot be read. The stream version may
    /// move the stream cursor. This only checks the header is a valid png header and the lib is
    /// functional
    /// </summary>
    public new int CanReadFile(string fname)
    {
        #if NET8_0_OR_GREATER
        return vtkPNGReader_CanReadFile(this.NativePointer, fname);
        #else
        return vtkPNGReader_CanReadFile(this.NativePointer, VtkString.ToNullTerminatedUtf8(fname));
        #endif
    }

    #region Interop
    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern nint vtkPNGReader_New();

#if NET8_0_OR_GREATER
    [LibraryImport(InteropInfo.NativeLibraryName, StringMarshalling = StringMarshalling.Utf8)]
    private static partial int vtkPNGReader_CanReadFile(nint self, string fname);
#else
    [DllImport(InteropInfo.NativeLibraryName)]
    private static extern int vtkPNGReader_CanReadFile(nint self, byte[] fname);
#endif
    #endregion
}
